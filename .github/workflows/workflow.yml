name: Common
on: push

jobs:
  always_run:
    name: Always Run
    runs-on: ubuntu-latest
    steps:
      - name: Check inputs
        run: echo "##[set-output name=branch;]$(echo ${GITHUB_REF#refs/heads/})"
